apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: actions-dev

resources:
  - ../../base

images:
  - name: shinobislayer/actions
    newName: shinobislayer/actions
    newTag: 2025.12.026-MAIN

namePrefix: dev-

labels:
  - pairs:
      environment: dev

patches:
  - path: ingress-patch.yaml
