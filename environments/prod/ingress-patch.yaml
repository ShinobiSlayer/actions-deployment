apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: actions-ingress
spec:
  rules:
  - host: actions-prod.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: actions-service
            port:
              number: 80
